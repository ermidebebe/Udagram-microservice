language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version # print the version for logging
  - docker build -t udagram-api-feed ./udagram-api-feed
  - docker tag udagram-api-feed ermiyad/udagram-api-feed:v1
  - docker build -t udagram-api-user ./udagram-api-user
  - docker tag udagram-api-user ermiyad/udagram-api-user:v1
  - docker build -t udagram-frontend ./udagram-api-frontend
  - docker tag udagram-api-frontend ermiyad/udagram-api-frontend:v1

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push ermiyad/udagram-api-feed:v1
  - docker push ermiyad/udagram-api-user:v1
  - docker push ermiyad/udagram-api-frontend:v1